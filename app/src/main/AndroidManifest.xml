<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

	<!-- 인터넷 사용 권한 설정, 카카오 SDK 사용-->
	<uses-permission android:name="android.permission.INTERNET" />

	<!-- Overlay 권한 설정, 차단창 띄우기 위해 필요 -->
	<uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

	<!-- 앱 사용량 통계 권한 설정, 앱 사용량 모니터링을 위해 필요 -->
	<uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
		tools:ignore="ProtectedPermissions" />

	<!-- 정확한 알람 설정 권한 설정, 타이머 알림을 위해 필요 -->
	<uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:name="com.yapp.breake.BreakeApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
		android:theme="@style/Theme.Breake"
        tools:targetApi="31" >

		<receiver
			android:name=".core.alarm.notification.NotificationReceiver"
			android:enabled="true"
			android:exported="false" />

		<service
			android:name=".core.detection.AppLaunchDetectionService"
			android:exported="false"
			android:label="@string/app_name"
			android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
			<intent-filter>
				<action android:name="android.accessibilityservice.AccessibilityService" />
			</intent-filter>
			<meta-data
				android:name="android.accessibilityservice"
				android:resource="@xml/accessibility_service_config" />
		</service>
	</application>

	<!-- 런처 아이콘을 갖는 모든 앱 쿼리 허용 -->
	<queries>
		<intent>
			<action android:name="android.intent.action.MAIN"/>
			<category android:name="android.intent.category.LAUNCHER"/>
		</intent>
	</queries>

</manifest>
